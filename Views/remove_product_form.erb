Delete a Product Record

<h1> Current Inventory </h1>
<ul>
   <% Club.all_as_objects.each do |club| %>
     <li>Store ID -<%= club.storeid %>    Club Code -<%=club.clubid%></li>
   <% end %>
</ul>

<form action="/remove_product">
 <p1>What is the Store ID of the product currently?</p1>
  <select name="old_store">
    <% Club.all_as_objects.each do |club| %>
      <option value="<%= club.storeid %>"><%= club.storeid %></option>
    <% end %>
  </select>
    <br>
  <p2>What is the Club Code of the product you wish to remvoe?</p2>
    <select name="club_id">
      <% Club.all_as_objects.each do |club| %>
        <option value="<%= club.clubid %>"><%= club.clubid %></option>
        
      <% end %>
    </select>
  

   <br> 
  <input type="submit">
</form>